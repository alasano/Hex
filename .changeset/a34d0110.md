---
"hex-app": minor
---

Add OpenAI-compatible provider support for AI Transforms: choose between OpenAI and any OpenAI-compatible Chat Completions endpoint (e.g. Cerebras) with per-provider API key and model, plus a configurable base URL
